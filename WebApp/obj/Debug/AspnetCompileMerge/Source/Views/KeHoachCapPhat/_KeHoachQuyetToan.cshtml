<form id="formDmTrangPhuc" enctype="multipart/form-data" name="formDmTrangPhuc">
    <!-- Modal Header -->
    <div class="modal-header">
        <h4 class="modal-title">Chọn kế hoạch quyết toán</h4>
        <button type="button" class="bootbox-close-button close" ng-click="cancel()">×</button>
    </div>

    <!-- Modal body -->
    <div class="modal-body" style="max-height: calc(100vh - 160px);">
        <div class="main-action">
            <div class="form-search w-100">
                <input type="text" placeholder="Tìm kiếm" ng-model="txtSearchSanPham">
                <button class="bg-green" style="color:white" ng-click="searchSanPham()">
                    <i class="fa fa-search" aria-hidden="true"></i>
                </button>
            </div>
        </div>
        <div class="content-table" style="max-height: calc(100vh - 255px); overflow: auto;">

            <div ng-show="alertMessage" class="text-center ">
                <span class="font-italic text-warning">Không có Kế hoạch còn nợ</span>
                <span ng-click="showHideMessage()"> (x) </span>
            </div>

            <table class="table-responsive table-hover result-point">
                <thead class="point-table-head">
                    <tr>
                        <th class="center" style="width:5%">
                            <label class="pos-rel">
                                <input type="checkbox" class="ace" ng-model="selectAllKeHoach" ng-click="checkAllDsKeHoach()" />
                                <span class="lbl"></span>
                            </label>
                        </th>
                        <th class="center">TT</th>
                        <th class="text-left">Số kế hoạch</th>
                        <th class="center">Ngày tạo</th>
                        <th class="text-left">Lý do cấp</th>
                    </tr>
                </thead>
                <tbody class="">
                    <tr ng-repeat="item in ListKHCapPhatConNo" ng-click="ShowThongTinDSSanPhamByKeHoach(item.ID_KH)">
                        <td class="text-center tb-ip">
                            <label class="pos-rel">
                                <input type="checkbox" class="ace" ng-model="item.Selected" ng-click="checkValDsKeHoachNo($index)"/>
                                <span class="lbl"></span>
                            </label>
                        </td>
                        <td>{{$index + 1}}</td>
                        <td>{{item.SO_KH}}</td>
                        <td>{{item.NGAY_TAO_TEXT}}</td>
                        <td>{{item.MO_TA}}</td>
                    </tr>

                </tbody>
            </table>
        </div>

        <div class="content-table" style="max-height: calc(100vh - 255px); overflow: auto;">
            <table class="table-responsive table-hover result-point">
                <thead class="point-table-head">
                    <tr>
                        <th class="center" style="width:5%">
                            <label class="pos-rel">
                                <input type="checkbox" class="ace" ng-model="selectAllSP" ng-click="checkAllDsSanPhamNo()" />
                                <span class="lbl"></span>
                            </label>
                        </th>
                        <th class="center">TT</th>
                        <th class="text-left">Mã hàng</th>
                        <th class="center">Tên hàng</th>
                        <th class="text-left">ĐVT</th>
                        <th class="text-left">Số lượng Nợ</th>
                        <th class="text-left">Số Q.toán</th>
                    </tr>
                </thead>
                <tbody class="">
                    <tr ng-repeat="item in ListDsSanPhamConNoByKeHoach">
                        <td class="text-center tb-ip">
                            <label class="pos-rel">
                                <input type="checkbox" class="ace" ng-model="item.Selected" ng-click="checkValDsSanPhamNo($index)" />
                            </label>
                        </td>
                        <td>{{$index + 1}}</td>
                        <td>{{item.MA_H55}}</td>
                        <td>{{item.TEN_SP}}</td>
                        <td>{{item.TEN_DVT}}</td>
                        <td>{{item.SL_NO_CON_LAI}}</td>
                        <td>
                            <input class=" form-control" type="number" ng-model="item.SL_TRA_NO" ng-init="item.SL_TRA_NO = item.SL_NO_CON_LAI"  />
                        </td>
                    </tr>

                </tbody>
            </table>
        </div>
    </div>


    <!-- Modal footer -->
    <div class="modal-footer main-action">
        <button class="btn-action-m bg-green" ng-click="btnQuyetToan()">
            <i class="fa fa-floppy-o" aria-hidden="true"></i> <span>Quyết toán</span>
        </button>
        <button class="btn-action-m bg-orange" ng-click="cancel()">
            <i class="fa fa-times" aria-hidden="true"></i>    <span> Đóng</span>
        </button>
    </div>
</form>

