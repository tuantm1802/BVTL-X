
@{
    ViewBag.Title = "Index";
}
<style>
    .row {
        margin-left: 0px;
        margin-right: 0px;
    }
    .form-control {
        padding: 0px 0px;
    }
    #resizable {
        height: 87vh
    }
    .table-responsive {
        display: table
    }
</style>

<nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Home/Index">Trang chủ</a></li>
            <li class="breadcrumb-item active" aria-current="page">Tồn kho</li>
        </ol>
    </nav>
<div class="container-fluid d-flex cons-ac-system" style=" margin-top: 0px " ng-controller="TonKhoController">
    @Html.Partial("~/Views/TonKho/HangDoiTonKho.cshtml")
    <div class="content-system no-border pb-0">
        <div class="main-action d-flex align-items-center justify-content-between flex-wrap">
            <button class="btn-action-m bg-green">
                <img src="~/App_Assets/images/l7_hover.svg" alt="">
                <span>In báo cáo</span>
            </button>
        </div>
        <div class="content-table mt-2">
            <table class="table-responsive table-hover result-point">
                <thead class="point-table-head">
                <tr>
                    <th style="width:10%;">Mã hàng<i id="or_dv"></i></th>
                    <th style="width:30%;">Tên hàng<i id="or_dv"></i></th>
                    <th style="width:10%;">ĐVT<i id="or_dv"></i></th>
                    <th style="width:10%;">Số lượng tồn<i id="or_ng"></i></th>
                    <th style="width:10%;">Giá trị tồn<i id="or_ng"></i></th>
                </tr>
                </thead>
                <tbody style="background-color:white">
                <tr ng-repeat="item in dsTonKho">
                    <td>{{item.CODE}}</td>
                    <td>{{item.NAME}}</td>
                    <td>{{item.DVT}}</td>
                    <td>{{item.SO_LUONG_TON}}</td>
                    <td>{{item.GIA_TRI_TON}}</td>
                </tr>
                </tbody>
            </table>
            <div class="row content-table pull-right">
                <div class="pages-switch">
                    <ul class="pagination pagination-sm m-0 float-right no-margin" uib-pagination total-items="SoLuongKetQua" items-per-page=10 ng-model="currentPage" ng-change="TimKiem(currentPage)" max-size=3 boundary-links="true" rotate="false" previous-text="&lsaquo;" next-text="&rsaquo;" first-text="&laquo;" last-text="&raquo;"></ul>
                </div>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <script src="~/app/Controller/Kho/TonKhoController.js"></script>
    <script>
        $("#menu-toggle").click(function (e) {
            e.preventDefault();
            $("#wrapper").toggleClass("active");
            if ($("#img-toggle").hasClass("img-normal")) {
                $("#img-toggle").removeClass("img-normal").addClass("img-180-rotate");
            }
            else if ($("#img-toggle").hasClass("img-180-rotate")) {
                $("#img-toggle").removeClass("img-180-rotate").addClass("img-normal");
            }
        });
    </script>
}
