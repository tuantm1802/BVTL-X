<div class="col-sm-12">
    <div class="row">
        @*<div>
            <button class="btn-action-m bg-blue-grey" ng-click="themThanhVien()" ng-show="appCodeType1">
                <span style="color:white">Thêm thành viên</span>
            </button>
            <button class="btn-action-m bg-blue-grey" ng-click="xoaThanhVien()" ng-show="appCodeType1">
                <span style="color:white">Xóa thành viên</span>
            </button>
        </div>*@
        <div class="content-table mt-2">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th class="text-center">
                            <input type="checkbox" ng-model="selectAll" ng-click="checkAll()" />
                        </th>
                        <th class="text-center">STT</th>
                        <th class="text-center">Họ tên</th>
                        <th class="text-center">Chức vụ</th>
                        <th class="text-center">Vị trí</th>
                        <th style="display: none">ID</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="item in listThanhVien track by $index">
                        <td class="text-center">
                            <label class="pos-rel">
                                <input type="checkbox" class="ace" ng-model="item.SELECT" value="10" />
                                <span class="lbl"></span>
                            </label>
                        </td>
                        <td>{{$index + 1}}</td>
                        <td>{{item.TEN_TV}}</td>
                        <td>{{item.CV_TV}}</td>
                        <td>{{item.CB_TV}}</td>
                        <td style="display: none">{{item.ID_BD_TV}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>